import { StyleSheet } from 'react-native';

export const questionsStyles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: "center"
    },
    question: {
        fontWeight: "bold"
    }
})